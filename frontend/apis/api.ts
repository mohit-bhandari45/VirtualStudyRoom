const baseURL = "http://localhost:8000";

const signUpRoute = `${baseURL}/auth/signup`;
const loginRoute = `${baseURL}/auth/login`;
const googleRoute = `${baseURL}/auth/google`;

export { signUpRoute, loginRoute, googleRoute };